"""
User interfaces for structure_generator.

This package contains all entrypoints that interact with users:
- CLI (argparse)
- GUI (Tkinter, PyQt, etc.)

These interfaces depend on the core, never the other way around.
"""
